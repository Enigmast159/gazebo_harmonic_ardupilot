# Понятный отчёт по прогону `final_exp_1`

## Что это за файл

Этот файл сделан как краткая человеко-читаемая сводка.

Важно:

- `run_evaluation.json` в текущем проекте считает только качество карты высот и latency mapper-а;
- ошибки по траектории в стандартный evaluator сейчас не входят;
- поэтому здесь отдельно добавлен раздел по траектории.

## Статус миссии

- Итоговый статус: `landing`
- Достигнуто waypoint-ов: `19 / 19`
- Длительность миссии: `490.162 с`

## Траектория

Ниже траектория сравнивается с плановой ломаной по waypoint-ам в горизонтальной плоскости `x/y`.

- Среднее отклонение: `0.043836 м`
- `p95`: `0.141473 м`
- Максимум: `0.496388 м`
- Доля сэмплов с ошибкой `<= 0.25 м`: `98.90%`

Вывод по требованию `не более 0.25 м`:

- если трактовать требование строго как предел для каждого сэмпла, то это `FAIL`, потому что максимум `0.496388 м`;
- если смотреть на типичное поведение, траектория в целом хорошая, потому что `p95 = 0.141473 м`.

Дополнительно по высоте полёта во время sweep:

- средняя абсолютная ошибка относительно плановых `15 м`: `2.549355 м`
- `p95`: `3.594392 м`
- максимум: `5.617229 м`

## Карта высот

Эти метрики берутся из `run_evaluation.json` и считаются относительно `terrain_ground_truth.csv`.

- Покрытие: `66046 / 66049` ячеек (`0.999955`)
- Средняя signed-ошибка: `-0.131135 м`
- `MAE`: `1.682143 м`
- Медианная абсолютная ошибка: `1.434423 м`
- `RMSE`: `2.086671 м`
- `p95` абсолютной ошибки: `4.024678 м`
- Максимальная абсолютная ошибка: `8.936700 м`
- Доля ячеек с ошибкой `<= 0.2 м`: `7.7991%` (`5151` ячеек)

Вывод по требованию `точность не хуже 0.2 м по высоте`:

- `FAIL`

## Короткий итог

- По маршруту прогон выглядит рабочим, но формально не проходит жёсткий порог `0.25 м` из-за редких выбросов.
- По карте высот прогон не проходит требование: ошибки слишком велики почти по всем основным метрикам.
