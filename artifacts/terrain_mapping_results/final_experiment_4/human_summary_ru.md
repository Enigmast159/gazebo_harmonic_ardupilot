# Понятный отчёт по прогону `final_experiment_4`

## Что это за файл

Этот файл сделан как краткая человеко-читаемая сводка.

Важно:

- `run_evaluation.json` в текущем проекте считает только качество карты высот и latency mapper-а;
- ошибки по траектории в стандартный evaluator сейчас не входят;
- поэтому здесь отдельно добавлен раздел по траектории.

## Статус миссии

- Итоговый статус: `landing`
- Достигнуто waypoint-ов: `19 / 19`
- Длительность миссии: `264.502 с`

## Траектория

Ниже траектория сравнивается с плановой ломаной по waypoint-ам в горизонтальной плоскости `x/y`.

- Среднее отклонение: `0.046785 м`
- `p95`: `0.158234 м`
- Максимум: `0.568632 м`
- Доля сэмплов с ошибкой `<= 0.25 м`: `99.44%`

Вывод по требованию `не более 0.25 м`:

- если трактовать требование строго как предел для каждого сэмпла, то это `FAIL`, потому что максимум `0.568632 м`;
- если смотреть на типичное поведение, траектория очень близка к требованию, потому что `p95 = 0.158234 м`.

Дополнительно по высоте полёта во время sweep:

- средняя абсолютная ошибка относительно плановых `15 м`: `2.542405 м`
- `p95`: `3.482982 м`
- максимум: `5.181950 м`

## Карта высот

Эти метрики берутся из `run_evaluation.json` и считаются относительно `terrain_ground_truth.csv`.

- Покрытие: `66048 / 66049` ячеек (`0.999985`)
- Средняя signed-ошибка: `+0.033206 м`
- `MAE`: `0.091350 м`
- Медианная абсолютная ошибка: `0.037352 м`
- `RMSE`: `0.283176 м`
- `p95` абсолютной ошибки: `0.265040 м`
- Максимальная абсолютная ошибка: `6.324799 м`
- Доля ячеек с ошибкой `<= 0.2 м`: `92.0043%` (`60767` ячеек)

Вывод по требованию `точность не хуже 0.2 м по высоте`:

- по средней абсолютной ошибке карта проходит;
- по `RMSE` и `p95` ещё есть небольшой недобор;
- если использовать строгий порог `0.2 м` как основную formal-метрику, то сейчас это всё ещё `FAIL`.

## Короткий итог

- По маршруту прогон уже очень хороший, но формально остаются редкие выбросы выше `0.25 м`.
- По карте высот это сильный результат: средняя ошибка уже маленькая, но редкие крупные выбросы всё ещё тянут вверх `RMSE`, `p95` и максимум.
